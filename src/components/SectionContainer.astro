---
const { class: className, id } = Astro.props
---

<!-- Para reutilizar lo de las secciones y centrado. -->

<section
  id={id}
  data-section={id}
  class={`section ${className} scroll-m-20 w-full mx-auto container lg:max-w-4xl md:max-w-2xl`}
>
  <slot />
</section>
  
